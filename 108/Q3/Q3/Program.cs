using System;
using System.Text;

namespace owo
{
    class main
    {
        static void Main(string[] args)
        {/*
            //         List<string> indata = Console.ReadLine().Split(' ').Where(v => v != "").ToList();    
            List<char> word = new List<char>();
            Encoding encoding = Encoding.ASCII;
            int a = int.Parse(Convert.ToString(0xabcd, 10));
            int b = int.Parse(Convert.ToString(0xcdef, 10));
            int c = int.Parse(Convert.ToString(0x2266, 10));
            int d = int.Parse(Convert.ToString(0xceed, 10));
            int e = int.Parse(Convert.ToString(0xaccd, 10));
            int k = int.Parse(Convert.ToString(0x5a82, 10));
            int f = b + c + k;
            List<int> count_list = new List<int>();
            count_list.Add(a);
            count_list.Add(b);
            count_list.Add(c);
            count_list.Add(d);
            count_list.Add(e);
            List<int> h_list = count_list;
            */
            for (int i = 0; i < 5; i++)
            {   
            }

        }

    }
}